Пусть у нас есть сортированный вектор с длинными последовательностями одинаковых чисел.
Представим его в виде списка пар вида (value, count).
Например [(1, 3), (2, 4)] является представлением вектора [1, 1, 1, 2, 2, 2, 2].

Требуется написать функцию, которая считает скалярное произведение двух векторов в такой форме.
